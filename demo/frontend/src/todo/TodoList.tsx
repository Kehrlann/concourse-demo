import * as React from 'react';
import { TodoItem } from './TodoItem';

export class TodoList extends React.Component<{todos: TodoItem[]}, {}> {
    render() {
        // const displayedTodos = this.props.todos.map(t => );
        return '';
    }
}