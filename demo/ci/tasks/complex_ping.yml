---
inputs:
- name: in

outputs:
- name: out

platform: linux
image_resource:
  type: docker-image
  source:
    repository: "alpine"
params:
  COUNT: 1
run:
  path: "/bin/ash"
  args:
  - "-c"
  - |
    ping -c $COUNT $(cat in/address.txt) > out/output.log
