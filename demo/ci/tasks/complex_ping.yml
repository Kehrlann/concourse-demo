---
inputs:
- name: in

outputs:
- name: out

platform: linux
image_resource:
  type: docker-image
  source:
    repository: "alpine"
run:
  path: "/bin/ash"
  args:
  - "-c"
  - |
    ping -c 1 $(cat in/address.txt) > out/output.log
